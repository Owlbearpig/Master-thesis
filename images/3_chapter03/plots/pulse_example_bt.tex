\documentclass{standalone}
\usepackage{tikz}
\usepackage{tikz-3dplot}
\usepackage{pgfplots}
\usepackage{pgfplotstable}
\usepgfplotslibrary{fillbetween}
\usepackage{siunitx}
\DeclareSIUnit{\arbitraryunit}{arb.\ unit}
\pgfplotsset{compat=1.5}

\begin{document}

\pgfplotsset{
    table/search path={plot_data/3_chapter03},
}

\begin{tikzpicture}
	\begin{axis}[
	    %scale only axis,
        %ymin=3.02, ymax=3.17,
        %xmin=1838, xmax=1842,
        %axis y line*=left,
        no markers,
        every axis plot/.append style={},
        axis line style = very thick,
        every tick/.style={black, very thick},
        xlabel=Time (\si{\pico \second}),
        ylabel=Amplitude (\si{\arbitraryunit}),
        %anchor=origin,
        legend entries={},
        legend style={legend columns=2, at={(0.04,0.95)}, anchor=north west,
                      nodes={scale=0.7, transform shape}}
        ]
        
    	\addplot[red] table [
    	x expr=\thisrowno{1},
    	y expr=\thisrowno{2}*10^9,
    	col sep=comma] 
    	{ref_BT_example.csv};
	\end{axis}
\end{tikzpicture}

\begin{tikzpicture}[remember picture]
	\begin{axis}[
	    %scale only axis,
        %ymin=3.02, ymax=3.17,
        xmax=1.25,
        %axis y line*=left,
        no markers,
        every axis plot/.append style={},
        axis line style = very thick,
        every tick/.style={black, very thick},
        xlabel=Frequency (\si{\tera \hertz}),
        ylabel=Spectral power (\si{\decibel}),
        %anchor=origin,
        legend entries={},
        legend style={legend columns=2, at={(0.04,0.95)}, anchor=north west,
                      nodes={scale=0.7, transform shape}}
        ]
        
    	\addplot[red] table [
    	x expr=\thisrowno{1},
    	y expr=\thisrowno{2},
    	col sep=comma] 
    	{ref_BT_example_fft.csv};

	\end{axis}
\end{tikzpicture}


\end{document}
