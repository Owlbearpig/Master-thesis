\documentclass{standalone}
\usepackage{tikz}
\usepackage{tikz-3dplot}
\usepackage{pgfplots}
\usepgfplotslibrary{fillbetween}
\usepackage{siunitx}
\pgfplotsset{compat=1.5}

\begin{document}


\pgfplotsset{
    table/search path={plot_data/5_chapter05/polymer},
}

\begin{tikzpicture}
	\begin{axis}[
	    %ymode=log,
        %ymin=-1.1, ymax=1.1,
        %xmin=0.2, xmax=2.5,
        %height=11cm,
        no markers,
        every axis plot/.append style={very thick},
        axis line style = very thick,
        every tick/.style={black, very thick},
        %yticklabel style={text width=width("$-0.5$")},
        xlabel=Frequency (\si{\giga \hertz}),
        ylabel=$1-V_c$,
        %anchor=origin,
        legend entries={{Result 3}},
        legend style={legend columns=1, at={(0.6,0.9)}, anchor=north west,
                      nodes={scale=0.90, transform shape}},
        reverse legend,
        ]
	    
	    \addplot[red] table [
    	x expr=\thisrowno{1}*10^-9,
    	y expr=1-\thisrowno{2},
    	col sep=comma] 
    	{lp_cp_deg.csv};
    	
	\end{axis}
\end{tikzpicture}
\begin{tikzpicture}

	\begin{axis}[
	    %ymode=log,
        %ymin=-1.1, ymax=1.1,
        %xmin=0.2, xmax=2.5,
        %height=11cm,
        no markers,
        every axis plot/.append style={very thick},
        axis line style = very thick,
        every tick/.style={black, very thick},
        %yticklabel style={text width=width("$-0.5$")},
        xlabel=Frequency (\si{\giga \hertz}),
        ylabel=$V_l$,
        %anchor=origin,
        legend entries={{Result 3}},
        legend style={legend columns=1, at={(0.6,0.9)}, anchor=north west,
                      nodes={scale=0.90, transform shape}},
        reverse legend,
        ]
	    
	    \addplot[red] table [
    	x expr=\thisrowno{1}*10^-9,
    	y expr=\thisrowno{2},
    	col sep=comma] 
    	{lp_lp_deg.csv};
    	
	\end{axis}
\end{tikzpicture}
\end{document}
