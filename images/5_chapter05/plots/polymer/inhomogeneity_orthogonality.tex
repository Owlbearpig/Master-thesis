\documentclass{standalone}
\usepackage{tikz}
\usepackage{tikz-3dplot}
\usepackage{pgfplots}
\usepgfplotslibrary{fillbetween}
\usepackage{siunitx}
\pgfplotsset{compat=1.5}

\begin{document}

\pgfplotsset{
    table/search path={plot_data/5_chapter05/polymer},
}

\begin{tikzpicture}
	\begin{axis}[
	    %ymode=log,
        %ymin=-55, ymax=5,
        %xmin=0.1, xmax=2.6,
        no markers,
        every axis plot/.append style={very thick},
        axis line style = very thick,
        every tick/.style={black, very thick},
        xlabel=Frequency (\si{\giga \hertz}),
        ylabel=Inhomogeneity,
        %anchor=origin,
        legend entries={{Result 3}},
        legend style={legend columns=1, at={(0.6,0.9)}, anchor=north west,
                     nodes={scale=0.90, transform shape}},
        %reverse legend,
        ]
	    
        \addplot[red] table [
    	x expr=\thisrowno{1}*10^-9,
    	y expr=\thisrowno{2},
    	col sep=comma] 
    	{inhomogeneity.csv};
    	
	\end{axis}
\end{tikzpicture}

\pgfplotsset{
    table/search path={plot_data/5_chapter05/polymer/eigenstate_params},
}

\begin{tikzpicture}
	\begin{axis}[
	    %ymode=log,
        %ymin=-55, ymax=5,
        %xmin=0.1, xmax=2.6,
        no markers,
        every axis plot/.append style={very thick},
        axis line style = very thick,
        every tick/.style={black, very thick},
        xlabel=Frequency (\si{\giga \hertz}),
        ylabel=Orthogonality deviation (deg),
        %anchor=origin,
        legend entries={{Result 3}},
        legend style={legend columns=1, at={(0.6,0.9)}, anchor=north west,
                     nodes={scale=0.90, transform shape}},
        %reverse legend,
        ]
	    
        \addplot[red] table [
    	x expr=\thisrowno{1}*10^-9,
    	y expr=90-(\thisrowno{2}-\thisrowno{3}),
    	col sep=comma] 
    	{eigenstate_azimuths.csv};
    	
	\end{axis}
\end{tikzpicture}
\end{document}
