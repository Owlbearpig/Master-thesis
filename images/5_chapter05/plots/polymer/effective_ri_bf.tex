\documentclass{standalone}
\usepackage{tikz}
\usepackage{tikz-3dplot}
\usepackage{pgfplots}
\usepackage{pgfplotstable}
\usepgfplotslibrary{fillbetween}
\usepackage{siunitx}
\pgfplotsset{compat=1.5}

\begin{document}

\pgfplotsset{
    table/search path={plot_data/5_chapter05/polymer},
}

\begin{tikzpicture}
	\begin{axis}[
	    name=ax1,
        ymin=1.18, ymax=1.34,
        xmin=71, xmax=115,
        axis y line*=left,
        no markers,
        every axis plot/.append style={ultra thick},
        axis line style = very thick,
        every tick/.style={black, very thick},
        xlabel=Frequency (\si{\giga \hertz}),
        ylabel=Refractive index,
        anchor=origin,
        legend entries={{$n_p$, slow axis}, {$n_s$, fast axis}, {$\Delta n$}},
        legend style={legend columns=1, at={(0.5,0.95)}, anchor=north west,
                      nodes={scale=0.7, transform shape}}
        ]
        
    	\addplot[red] table [
    	x expr=\thisrowno{1}*10^-9,
    	y expr=\thisrowno{3},
    	col sep=comma] 
    	{effective_RI.csv};
    	
    	\addplot[blue] table [
    	x expr=\thisrowno{1}*10^-9,
    	y expr=\thisrowno{2},
    	col sep=comma] 
    	{effective_RI.csv};
	    
	    % fake plot to trigger legend entry thingy
	    \addplot[green] coordinates {(0,0)};
	    
	\end{axis}
	\begin{axis}[
    	xmin=71, xmax=115,
    	%ymin=1.18, ymax=1.34,
    	axis y line*=right, 
    	axis x line=none,
    	no markers,
        every axis plot/.append style={ultra thick},
        axis line style = very thick,
        every tick/.style={black, very thick},
    	ylabel=Birefringence (green)]
    	
        \addplot[green] table [
        x expr=\thisrowno{1}*10^-9,
    	y expr=\thisrowno{4},
    	col sep=comma]
        {effective_RI.csv};

    \end{axis}
\end{tikzpicture}
\begin{tikzpicture}
    \begin{axis}[
        axis line style = very thick,
        yticklabels={,,},
        xticklabels={,,},
        ytick style={draw=none},
        xtick style={draw=none},
        ]
    \end{axis}

    \begin{axis}[
        axis y line*=right,
        no markers,
        every axis plot/.append style={ultra thick},
        axis line style = very thick,
        every tick/.style={black, very thick},
        xlabel=Frequency (\si{\giga \hertz}),
        ylabel=Absorption coefficient $\left(\si{\per\centi\meter}\right)$,
        legend entries={{$n_p$, slow axis}, {$n_s$, fast axis}},
        legend style={legend columns=1, at={(0.5,0.95)}, anchor=north west,
                      nodes={scale=0.75, transform shape}}
        ]
	    
        \addplot[red] table [
    	x expr=\thisrowno{1}*10^-9,
    	y expr=\thisrowno{6},
    	col sep=comma] 
    	{effective_RI.csv};

    	\addplot[blue] table [
    	x expr=\thisrowno{1}*10^-9,
    	y expr=\thisrowno{5},
    	col sep=comma]
    	{effective_RI.csv};
	\end{axis}
\end{tikzpicture}
\end{document}
