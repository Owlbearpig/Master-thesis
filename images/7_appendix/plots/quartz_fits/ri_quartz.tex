\documentclass{standalone}
\usepackage{tikz}
\usepackage{tikz-3dplot}
\usepackage{pgfplots}
\usepackage{pgfplotstable}
\usepgfplotslibrary{fillbetween}
\usepackage{siunitx}
\pgfplotsset{compat=1.5}

\begin{document}

\pgfplotsset{
    table/search path={plot_data/7_appendix/quartz_fits},
}

\begin{tikzpicture}
	\begin{axis}[
        %ymin=3.02, ymax=3.17,
        %xmin=0.25, xmax=2.7,
        %no markers,
        %scale only axis,
        every axis plot/.append style={thick},
        axis line style = very thick,
        every tick/.style={black, very thick},
        yticklabel style={/pgf/number format/.cd,
                     fixed,
                     fixed zerofill,
                     precision=3,
                     },
        xlabel=Frequency (\si{\tera \hertz}),
        ylabel=Refractive index,
        anchor=origin,
        legend style={legend columns=1, at={(0.1,0.8)}, anchor=north west,
                      nodes={scale=0.7, transform shape}}
        ]
	    
        \addplot[red, only marks, mark=o, mark size=1pt] table [
    	x expr=\thisrowno{0},
    	y expr=\thisrowno{1},
    	col sep=comma] 
    	{quartz_m_fast3.csv};
    	\addlegendentry{Data fast axis}
        
    	\addplot[red] table [
    	x expr=\thisrowno{1}*10^-12,
    	y expr=\thisrowno{4},
    	col sep=comma] 
    	{fast_grisch1990_fit.csv};
    	\addlegendentry{Fit fast axis}
	    
	\end{axis}
	
\end{tikzpicture}

\begin{tikzpicture}
	\begin{axis}[
        %ymin=3.02, ymax=3.17,
        %xmin=0.25, xmax=2.7,
        %no markers,
        every axis plot/.append style={thick},
        axis line style = very thick,
        every tick/.style={black, very thick},
        yticklabel style={/pgf/number format/.cd,
                     fixed,
                     fixed zerofill,
                     precision=3,
                     },
        xlabel=Frequency (\si{\tera \hertz}),
        ylabel=Refractive index,
        anchor=origin,
        legend style={legend columns=1, at={(0.1,0.8)}, anchor=north west,
                      nodes={scale=0.7, transform shape}}
        ]
	    
        \addplot[blue, only marks, mark=o, mark size=1pt] table [
    	x expr=\thisrowno{0},
    	y expr=\thisrowno{1},
    	col sep=comma] 
    	{quartz_m_slow3.csv};
    	\addlegendentry{Data slow axis}
    	
        \addplot[blue] table [
    	x expr=\thisrowno{1}*10^-12,
    	y expr=\thisrowno{4},
    	col sep=comma] 
    	{slow_grisch1990_fit.csv};
    	\addlegendentry{Fit slow axis}
	    
	\end{axis}
\end{tikzpicture}
\end{document}
